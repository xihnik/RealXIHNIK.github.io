export class Map {
    constructor(path) {
        // this.collection = [x];
        // for(let i =0;i<y;i++)
        // {
        //     this.collection[i] = [y];
        // }
        this.image = new Image();
        this.image.src = path;
    }
}